# Sistema de Internacionalización (i18n) - Glover Website

## 📋 Descripción General

Este documento describe el sistema de internacionalización implementado en el sitio web de Glover, que permite ofrecer contenido en **Español** e **Inglés**.

## 🛠 Tecnologías Utilizadas

- **react-i18next**: Librería de internacionalización para React
- **i18next**: Framework de internacionalización
- **i18next-browser-languagedetector**: Detección automática del idioma del navegador

## 📁 Estructura de Archivos

```
src/
├── config/
│   └── i18n.ts                    # Configuración principal de i18next
├── locales/                        # Archivos de traducción
│   ├── es/                         # Traducciones en español
│   │   ├── common.json            # Navegación, footer, botones
│   │   ├── home.json              # Página de inicio
│   │   ├── about.json             # Quiénes somos
│   │   ├── business.json          # Líneas de negocio
│   │   ├── contact.json           # Contacto
│   │   ├── sustainability.json    # Sustentabilidad
│   │   ├── processes.json         # Procesos tecnológicos
│   │   └── salesroom.json         # Sala de ventas
│   └── en/                         # Traducciones en inglés
│       ├── common.json            # (misma estructura que español)
│       ├── home.json
│       ├── about.json
│       ├── business.json
│       ├── contact.json
│       ├── sustainability.json
│       ├── processes.json
│       └── salesroom.json
└── components/
    └── LanguageSwitcher.tsx       # Selector de idioma
```

## 🚀 Características Implementadas

### ✅ A. Traducción de Contenido Existente

Todas las secciones del sitio han sido traducidas:
- Navegación principal y menús
- Footer con enlaces y contacto
- Página de inicio (Home)
- Quiénes Somos (About Us)
- Líneas de Negocio (Business Lines)
- Sustentabilidad (Sustainability)
- Procesos Tecnológicos (Technological Processes)
- Sala de Ventas (Showroom)
- Contacto (Contact)

### ✅ B. Configuración Multilingüe

- **Detección automática**: El sistema detecta el idioma del navegador del usuario
- **Persistencia**: El idioma seleccionado se guarda en localStorage
- **Idioma por defecto**: Español (fallback)
- **Cambio dinámico**: Los usuarios pueden cambiar de idioma sin recargar la página

### ✅ C. Selector de Idioma (Language Switcher)

El componente `LanguageSwitcher` está implementado con:
- **Ubicación**: Visible en el header tanto en desktop como móvil
- **Diseño**: 
  - Icono de globo terráqueo
  - Banderas y texto del idioma
  - Dropdown animado con Framer Motion
  - Indicador visual del idioma activo
- **Accesibilidad**: Texto claro sin depender únicamente de banderas

## 💻 Uso del Sistema

### Usar traducciones en un componente

```typescript
import { useTranslation } from 'react-i18next';

const MiComponente: React.FC = () => {
  // 'common' es el namespace por defecto
  const { t } = useTranslation('common');
  
  // Usar traducciones
  return (
    <div>
      <h1>{t('nav.home')}</h1>
      <p>{t('footer.tagline')}</p>
    </div>
  );
};
```

### Usar namespace específico

```typescript
import { useTranslation } from 'react-i18next';

const HomeComponent: React.FC = () => {
  // Especificar namespace
  const { t } = useTranslation('home');
  
  return (
    <div>
      <h1>{t('companyIntro.title')}</h1>
      <p>{t('companyIntro.paragraph1')}</p>
    </div>
  );
};
```

### Interpolación de variables

```typescript
// En el JSON:
{
  "footer": {
    "rights": "© {{year}} Industrial Glover. All rights reserved."
  }
}

// En el componente:
const year = new Date().getFullYear();
t('footer.rights', { year })
```

## ➕ Agregar Nuevas Traducciones

### Paso 1: Agregar en el archivo JSON

**Español** (`src/locales/es/common.json`):
```json
{
  "newSection": {
    "title": "Nuevo Título",
    "description": "Nueva descripción"
  }
}
```

**Inglés** (`src/locales/en/common.json`):
```json
{
  "newSection": {
    "title": "New Title",
    "description": "New description"
  }
}
```

### Paso 2: Usar en el componente

```typescript
const { t } = useTranslation('common');

return (
  <div>
    <h2>{t('newSection.title')}</h2>
    <p>{t('newSection.description')}</p>
  </div>
);
```

## 📝 Crear un Nuevo Namespace

Si necesitas agregar una nueva sección con muchas traducciones:

### 1. Crear archivos JSON

Crear:
- `src/locales/es/nuevaseccion.json`
- `src/locales/en/nuevaseccion.json`

### 2. Importar en la configuración

Editar `src/config/i18n.ts`:

```typescript
import nuevaseccionES from '../locales/es/nuevaseccion.json';
import nuevaseccionEN from '../locales/en/nuevaseccion.json';

const resources = {
  es: {
    // ... otros namespaces
    nuevaseccion: nuevaseccionES,
  },
  en: {
    // ... otros namespaces
    nuevaseccion: nuevaseccionEN,
  },
};

// Agregar al array de namespaces
i18n.use(initReactI18next).init({
  // ...
  ns: ['common', 'home', 'about', 'business', 'contact', 'sustainability', 'processes', 'salesroom', 'nuevaseccion'],
  // ...
});
```

### 3. Usar el nuevo namespace

```typescript
const { t } = useTranslation('nuevaseccion');
```

## 🔍 Debugging

### Ver el idioma actual

```typescript
const { i18n } = useTranslation();
console.log('Idioma actual:', i18n.language);
```

### Cambiar idioma programáticamente

```typescript
const { i18n } = useTranslation();
i18n.changeLanguage('en'); // o 'es'
```

### Verificar si una clave existe

```typescript
const { t } = useTranslation();
const exists = t('some.key', { defaultValue: false });
```

## 🎯 Mejores Prácticas

1. **Organización**: Mantén las traducciones organizadas por secciones lógicas
2. **Consistencia**: Usa la misma estructura de claves en español e inglés
3. **Nomenclatura**: Usa camelCase para las claves (`companyIntro.title`)
4. **Namespaces**: Separa traducciones por páginas o secciones grandes
5. **Interpolación**: Usa variables para contenido dinámico
6. **Contexto**: Agrupa traducciones relacionadas bajo un mismo objeto

## 🌐 Idiomas Soportados

| Código | Idioma  | Bandera |
|--------|---------|---------|
| `es`   | Español | 🇪🇸     |
| `en`   | Inglés  | 🇺🇸     |

## 📱 Responsive

El selector de idioma es completamente responsive:
- **Desktop**: Muestra icono, texto e idioma y bandera
- **Mobile**: Muestra solo el icono de globo para ahorrar espacio
- **Dropdown**: Se adapta a cualquier tamaño de pantalla

## 🔄 Persistencia

El idioma seleccionado se guarda en `localStorage` con la clave `i18nextLng`, por lo que persiste entre sesiones del navegador.

## ✅ Verificación de Implementación

Para verificar que todo funciona correctamente:

1. **Compilar el proyecto**: `npm run build`
2. **Iniciar el servidor de desarrollo**: `npm run dev`
3. **Probar el selector de idioma**: Cambiar entre español e inglés
4. **Verificar todas las páginas**: Navegar por todo el sitio en ambos idiomas
5. **Verificar localStorage**: Inspeccionar `localStorage.getItem('i18nextLng')`

## 🐛 Solución de Problemas

### Las traducciones no aparecen

1. Verificar que el archivo JSON tiene la sintaxis correcta
2. Asegurarse de que el namespace está importado en `i18n.ts`
3. Verificar que la clave existe en ambos idiomas

### El idioma no cambia

1. Verificar que `LanguageSwitcher` está correctamente integrado
2. Revisar la consola del navegador para errores
3. Limpiar el localStorage: `localStorage.removeItem('i18nextLng')`

### Errores de compilación

1. Verificar que todos los archivos JSON tienen sintaxis válida
2. Asegurarse de que todas las importaciones en `i18n.ts` son correctas
3. Ejecutar `npm install` para verificar que todas las dependencias están instaladas

## 📚 Recursos Adicionales

- [Documentación oficial de react-i18next](https://react.i18next.com/)
- [Documentación de i18next](https://www.i18next.com/)
- [Guía de mejores prácticas](https://react.i18next.com/latest/using-with-hooks)

---

**Nota**: Este sistema está diseñado para ser fácilmente extensible. Si necesitas agregar más idiomas en el futuro, simplemente sigue el mismo patrón de crear archivos JSON en una nueva carpeta `src/locales/[código-idioma]/` y agregar los imports en `i18n.ts`.
